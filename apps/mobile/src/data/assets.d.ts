// Metro asset modules used by the data layer. The wasm binary is registered
// as an asset (metro.config.js assetExts) and resolved to a URL with
// expo-asset's Asset.fromModule.
declare module '@sqlite.org/sqlite-wasm/sqlite3.wasm';
