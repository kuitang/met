/** Graph pipeline: snap route linestrings into a node/edge graph; assign galleryNumber by point-in-polygon; detect floor-transition edges (stairs/elevator) and door crossings → graph.json (nodes: id, lat/lon, floor, gallery; edges: length, kind). */
function main(): void {
  throw new Error("not implemented");
}

main();
