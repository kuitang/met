/** build-db: assemble met.sqlite — objects table + FTS5 (porter unicode61, prefix 2/3/4, external-content) + galleries/amenities/graph tables + gzipped GeoJSON blobs; optional LLM synonym expansion column; dated snapshot committed under data/snapshots/. */
function main(): void {
  throw new Error("not implemented");
}

main();
