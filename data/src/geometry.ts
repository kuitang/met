/** Geometry pipeline: fetch Living Map tile pyramid (z17-18, both sites), decode MVT via @mapbox/vector-tile, stitch/dedupe across tiles → galleries.geojson + routes.geojson + amenities.geojson; raw tiles cached under data/raw/. */
function main(): void {
  throw new Error("not implemented");
}

main();
