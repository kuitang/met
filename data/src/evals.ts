/** Evals: coverage (GalleryNumber↔polygon match %), geometry sanity, graph connectivity (Dijkstra success), GPS-mapping behavior, per-floor SVG visual diffs, and ~50 golden search query→objectID cases against the real met.sqlite — each emitting a markdown report under data/evals/reports/. */
function main(): void {
  throw new Error("not implemented");
}

main();
