# Node / Expo
node_modules/
.expo/
.expo-shared/
dist/
web-build/

# Native build output
ios/
android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# Metro
.metro-health-check*

# Env / secrets
.env
.env.*
!.env.example

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*
*.log

# OS / editor
.DS_Store
*.pem
.idea/
.vscode/

# Test artifacts
coverage/
playwright-report/
test-results/
.playwright-mcp/

# Planning-session browser screenshots (competitive-study scratch, not deliverables)
/met-explorer-*.png

# Large local caches (re-fetchable; see data/src/embed-images.ts)
data/raw/met-images/
data/raw/met-objects/

# Image-proxy disk cache (server/src/routes/img.ts; lives on the Fly volume in prod)
data/img-cache/

# Image-embedding vector index (~100 MB at full scale, rebuilt incrementally by
# data/src/embed-images.ts; server loads it from DATA_DIR — lives on the Fly
# volume in prod, never in git)
data/snapshots/image-embeddings/
